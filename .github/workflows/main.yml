name: Build and Test Static Site

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Confirm files exist
        run: |
          echo "Listing files:"
          ls -R

      - name: Success message
        run: echo "Static site workflow ran successfully!"
